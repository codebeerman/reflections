What happens when you initialize a repository? Why do you need to do it?
	When a repository is initialized, a new of that is created.
	It is necessary in order that the git can track the files, and register the commits.

How is the staging area different from the working directory and the repository? What value do you think it offers?
	The staging area is in the middle of the working directory and the repository. Is a stage before the repository. 
	It helps to choose the files you want commit one by one.

How can you use the staging area to make sure you have one commit per logical change?
	Just looking which files are in staging area.

What are some situations when branches would be helpful in keeping your history organized?
How would branches help?
	Branches would be helpful in situations that include beta versions of the project. 
	The branches can maintain the beta versions separated of the official version.

How do the diagrams help you visualize the branch structure?
	The diagrams shows us the logs of each branch.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
	Merging two branches, how you can expect, their differences are merged. In the diagram the neither branchs 
	disappear because bothstill exist.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
	When Git merges automatically it is more practical. But when don't then we have to analyse the code and
	make the appropriate changes.